x = [1.0, 2.0]
f(x) = x[1]^2 + 3x[1]*x[2] + 2x[2]^2 + sin(x[1])

function partial_derivative(f, x::Vector, i; h=1e-5)
    plus = copy(x)
    minus = copy(x)
    plus[i] = x[i] + h
    #println(plus)
    minus[i] = x[i] - h
    #println(minus)
    numerator = f(plus)-f(minus) 
    denominator = 2 * h 
    approx_partial_x = numerator / denominator
    return approx_partial_x
end

function gradient(f,x)
    grad = zeros(length(x))
    for i in eachindex(x)
        grad[i] = partial_derivative(f, x, i)
    end
    return grad
end

function Hessian(f, x,: h=1e-4)
    matrix = zeros(eachindex(x),eachindex(x))
    for i in eachrow(matrix)
        for j in eachcolumn(matrix)
            matrix[i,j]= partial_derivative(part)
println(gradient(f, x))
